# AnisPaper UI

The control room is the native Qt Quick app `anis-paper-ui`.

```bash
anis-paper-ui
```

It talks to `anis-paperd` over `$XDG_RUNTIME_DIR/anispaper.sock`. There is no Electron client.
